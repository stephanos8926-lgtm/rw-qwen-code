# Task Plan

- [ ] Analyze `Configuration.tsx` and `server.ts` to understand current settings handling.
- [ ] Define global vs. project-specific settings.
- [ ] Refactor `Configuration.tsx` to enforce scoping:
    - [ ] Only show relevant settings in each tab.
    - [ ] Save only relevant settings to the appropriate scope.
- [ ] Add description/tooltip for "Max Tokens".
- [ ] Update `server.ts` if necessary (though it seems to support scoping already).
- [ ] Verify changes.
